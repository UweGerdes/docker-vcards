{
  "name": "vCard",
  "description": "Gulp and other packages for vCard webapp development and testing",
  "version": "0.2.0",
  "keywords": [
    "vcard"
  ],
  "license": "MIT",
  "author": {
    "name": "Uwe Gerdes",
    "email": "entwicklung@uwegerdes.de"
  },
  "engines": {
    "node": ">=10.10.0"
  },
  "dependencies": {
    "express-validator": "^6.15.0",
    "libqp": "^2.0.1",
    "multer": "^1.3.0",
    "vcf": "^2.1.1"
  },
  "devDependencies": {
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/uwegerdes/docker-vcard"
  },
  "scripts": {
    "start": "gulp",
    "dev": "nodemon --watch gulpfile.js --watch gulp --watch configuration.yaml --delay 1 --exec gulp",
    "dev-quiet": "nodemon --watch gulpfile.js --watch gulp --quiet --exitcrash --delay 1 --exec gulp",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
