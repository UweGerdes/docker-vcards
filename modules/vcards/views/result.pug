ul#list.result-list
  each vcard in vcards
    li.list-item
      span.list-item-value= unCsv(vcard.getValue('n'))
      a.button.open(href='/vcards/' + vcard.id + '/') öffnen
      if id != vcard.id
        a.button.merge(href='/vcards/merge/' + id + '/' + vcard.id + '/') merge
