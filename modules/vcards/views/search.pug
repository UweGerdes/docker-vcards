h2.subheadline Suchen
form#searchForm.form(method='POST', action='/vcards/search/')
  .searchFieldsContainer
    each label, name in labels
      div.checkboxContainer
        input.checkbox(type='checkbox', name='searchFields', value=name, id='search_' + name)
        label(for='search_' + name)
          = label
  .searchStringContainer
    input.searchString(type='text', name='searchString', placeholder='Suchwort', value='test')
    input.searchSubmit(type='button', value='suchen...', onclick='searchSubmit();')
    input.searchSubmit(type='submit', value='suchen...')
#searchResult
